services:
  records:
    image: ghcr.io/fastenhealth/fasten-onprem:main
    container_name: whole-fasten
    restart: unless-stopped
    ports:
      - "9090:8080"
    volumes:
      - ./db:/opt/fasten/db
